"""Constants for the MAIN app"""

# Default currency settings
# These are used as fallbacks when vendor/product currency information is not available
DEFAULT_CURRENCY_CODE = 'NGN'
DEFAULT_CURRENCY_SYMBOL = '₦'  # Disambiguated symbol to avoid confusion
